---
description: API keys live in macOS Keychain (account=praxis), never plaintext
globs: **/.env, **/.env, **/.env.example
alwaysApply: false
---

# Standing order — API keys live in macOS Keychain (account=praxis), never plaintext

`decision_key`: `architecture-policy::auth::keychain-secrets`

(no rationale captured)

Source: `operator_decisions` row, `decision_kind=architecture_policy`.
Full record: `bin/praxis-agent praxis_operator_decisions --input-json '{"action":"get","decision_key":"architecture-policy::auth::keychain-secrets"}'`
