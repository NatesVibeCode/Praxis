# Active Next Moves

- Keep the smoke queue aligned with the seeded DB definition.
- Keep `praxis workflow native-operator smoke` proving terminal execution instead of only submission.
- Prefer receipt-backed proofs when extending native operator health surfaces.
- Reject new side channels that duplicate `workflow_runs`, receipts, or outbox truth.
