# receipts

Owns:

- immutable receipt recording
- immutable event recording
- evidence query helpers

Does not own:

- runtime control flow
- policy decisions

If an action matters, it should show up here.
