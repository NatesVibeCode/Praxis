# Wave 0 Gate — 2026-04-23

## Current coordination state

- `coordination_state`: `frozen`
- `captured_at`: `2026-04-23T21:35:20.333028+00:00`
- Kickoff backlog scope: `85` open bugs
- Replay-ready at kickoff: `4`
- Replay-blocked at kickoff: `81`
- Underlinked at kickoff: `81`

## Authority probes

- `praxis_orient`: pass
- `praxis_bugs(action=stats)`: pass
- `praxis_bugs(action=list, open_only=true, include_replay_state=true)`: pass
- `praxis_bugs(action=search, open_only=true)`: pass
- `praxis_replay_ready_bugs`: pass

These passes were captured through the canonical workflow tool surfaces in-process, not through `./scripts/praxis`, because the shell front door is currently tracked as an open blocker in `BUG-88BE517A`.

## Wave 0 scope

- Wave 0 contains `50` packets covering `63` bugs.
- The leading authority packet is `wave-0-authority-repair-authority-bug-system-cluster-authority-bug-system`.
- The first explicit blockers in that packet are:
- `BUG-30798A5F` — offline materialization imported DB-coupled tool modules
- `BUG-88BE517A` — `./scripts/praxis workflow` hangs for orient and bug commands
- `BUG-1FC820AC` — repo DB authority points at `host.docker.internal`, which does not resolve from the host shell

Additional Wave 0 clusters already in scope include bug evidence authority, DB authority hygiene, secret authority hygiene, contract drift, input-validation gaps, and operator-write/query surface failures.

## Exit gate for Wave 1

- The repo DB authority path must resolve without the `host.docker.internal` failure captured in `BUG-1FC820AC`.
- The canonical shell front door must return promptly for:
- `./scripts/praxis workflow orient --json`
- `./scripts/praxis workflow bugs stats --json`
- `./scripts/praxis workflow bugs list --limit 1 --json`
- Packet materialization must remain snapshot-only and offline-clean, without leaking DB authority failures on stderr.
- Bug writes must no longer silently tolerate underlinked closure paths. Wave 1 only starts once evidence recovery can happen truthfully instead of cosmetically.

## Residual risk if Wave 0 is skipped

- The program would appear organized while still depending on a broken shell front door.
- `81` kickoff bugs would remain replay-blocked or underlinked, which makes broad parallel fixing mostly theater.
- Later packet execution would inherit unstable authority and produce ambiguous receipts, weak closeouts, or silent hangs.
