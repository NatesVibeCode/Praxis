# Integrations

Use this area for external adapters only.

Examples:

- LLM provider adapters
- shell execution adapters
- test runner adapters
- human approval bridges
- API bindings

Do not let integrations become the owner of runtime truth.
