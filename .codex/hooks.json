{
  "$comment": "Codex Desktop shows a receipt for every matched PreToolUse hook invocation, even when the hook stdout is silent. To avoid transcript noise, project-local Codex config keeps only SessionStart active by default. The raw PreToolUse policy hook script remains available for opt-in/debug use, and Praxis operation calls still surface standing orders through the gateway.",
  "hooks": {
    "SessionStart": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "bash -c 'exec \"${CODEX_PROJECT_DIR:-${CLAUDE_PROJECT_DIR:-$PWD}}/.claude/hooks/session-start-standing-orders.sh\"'"
          }
        ]
      }
    ]
  }
}
