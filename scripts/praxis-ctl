#!/usr/bin/env bash
set -euo pipefail

SCRIPT_DIR="$(cd "$(dirname "$0")" && pwd)"

export PRAXIS_COMMAND_NAME="praxis-ctl"
export PRAXIS_SERVICE_MANAGER_PATH="scripts/praxis"
export PRAXIS_COMPATIBILITY_ALIAS_PATH="scripts/praxis-ctl"

exec "$SCRIPT_DIR/praxis-service-manager" "$@"
